# Chronicle — how a coding agent maps the key-forging work into THIS repo (2026-06-10)

**Status:** plan, not executed. Companions in this directory:
`OVERLAP_KEY_FORGING_X_V6_2026-06-10.md` (the conjecture-by-conjecture map) and
`V6_SKILLS_PASS_2026-06-10.md` (the day's mechanical pass — its edit rules
R1–R6 bind any skill work here). The register
(`agentprivacy-docs/research/CONJECTURE_REGISTER_V6.md`) is G1-gated:
**cite it, never edit it.**

## The mapping, step by step

1. **New skill: `privacy-layer/agentprivacy-key-forging`** (working_paper, v1.0).
   The gap it fills: UOR concepts are distributed (03-31 convergence) but no
   skill teaches the *mechanism*. Teaching spine (link out to the star repo per
   the dual-agent-harness coherence precedent — link, don't import):
   - canonical form → κ-label (`sha256:H(canonical form)`, kappa excluded from
     its own preimage) → **Law L5: verification is re-derivation**;
   - the carrier (PNG `tEXt` `cityKey` chunk — the image IS the key; every
     framing a full instance; re-encoding strips, the κ caption survives);
   - SHAPE canons (versioned pure functions of content: stance envelope ·
     SHAPE-1.5 engraving · the v2 figures ladder);
   - the eight stations (compression) and rehydration into the dual agents
     (the Swordsman forgets, the key remembers for him; the First Person never
     serialized);
   - conformance vectors as exercises: κ
     `sha256:0b4916babe5eb17104b342ab06030f2071a818024b345bf6d2e4115617c3c527`
     (default key, 303 bytes) · SHAPE-1.5 w(12)=0.90/w(63)=0.25/w(0)=0.10.
   - Conjecture anchors (register-true language only): **C87** (the Key
     Accumulates — this system is its pre-circuit substrate), C85 (load-bearing
     UI), C86 (operational amnesia), C44 (carrier makes it A/B-testable),
     C81/C84 (what the silhouette leaks — design constraint).
   - V6 rules compliance from birth: R(t)/conditional-regime sentence where
     ceilings are implied; 🪢 as colour-not-weight (regime 1); one register-note
     line.
2. **Placement:** `privacy-layer/` (it is mechanism, like disclosure-phi /
   three-axis-separation) — a registered skillset, so no `plugin.json` change
   beyond convention; mirror the directory shape of an existing privacy-layer
   skill (SKILL.md + references/ if needed).
3. **Touch existing skills** (one linking line each, R5-style, no rework):
   `vrc-identity` (C44 instrument) · `crypto-zkp` (the v3 κ-commitment path) ·
   `role/holonic-persistence` (rehydration as its worked example) ·
   `meta/agentprivacy-dual-agent-harness` (the trust-task runtime — pour →
   walk → witness → charge — as the human-scale instance of its loop) ·
   `persona/agentprivacy-algebraist` + `registry-keeper` (the 03-31 UOR
   convergence is now operational).
4. **MAPPING.md**: one row for the star repo (the walkable model · /sigil ·
   the carrier) as a live V6 surface — alongside the pending v5.5 structure
   decision.
5. **Chronicle the executed pass** in `chronicles/` (sibling to the
   dual-agent-harness chronicle).

## Ask-first decisions

The skill's final name (`key-forging` vs `content-addressed-identity`) · its
voice (the First Person's individual deep-pass is in progress — mechanical
creation is safe, voice-level teaching may want his hand) · whether the stray
`mnt/user-data/outputs/` tree is deleted first (flagged in the V6 pass) ·
anything that would put register numbers in frontmatter (keep them in body
text with register citations).

*The plugin teaches the law and the gate; now it can teach the forge.* `(⚔️⊥⿻⊥🧙)😊`
